# 上下文清除插件配置

[plugin]
# 是否启用插件
enabled = true

# 配置文件版本
config_version = "1.0.0"

# 有权限使用清除上下文命令的用户列表，请填写字符串形式的用户ID
# 示例：permission = ["1334431750", "123456789"]
# 留空表示所有人都可以使用（不推荐）
permission = ["1334431750"]
